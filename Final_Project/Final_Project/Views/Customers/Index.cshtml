@model IEnumerable<Customer>

<h1 class="h1Customer"  class="alert alert-info">
    Customer
</h1>
<br />
<hr />
<br />

<a asp-controller="Customers" asp-action="GetCreate" class="btn btn-success mb-1">Add Customer</a>

@if (Model.Count() > 0)
{
    <table class="table table-bordered table-striped table-hover" style="text-align:center">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(e=>e.Id)</th>
                <th>@Html.DisplayNameFor(e=>e.Name)</th>
                <th>@Html.DisplayNameFor(e=>e.Address)</th>
                <th>@Html.DisplayNameFor(e=>e.Mobile)</th>
                <th>@Html.DisplayNameFor(e=>e.Email)</th>
                <th></th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Address
                    </td>
                    <td>
                        @item.Mobile

                    </td>
                    <td>
                        @item.Email
                    </td>
                    <td>
                        <a asp-action="GetDetails" asp-controller="Customers" asp-route-id="@item.Id" class="btn btn-outline-success" title="Details"><i class="bi bi-list-ul"></i></a>
                    </td>
                    <td>
                        <a asp-action="GetEditView" asp-controller="Customers" asp-route-id="@item.Id" class="btn btn-outline-primary" title="Edit"><i class="bi bi-pencil-square"></i></a>
                    </td>
                    <td>
                        <a asp-action="GetDeleteView" asp-controller="Customers" asp-route-id="@item.Id" class="btn btn-outline-danger" title="Delete"><i class="bi bi-trash"></i></a>

                    </td>

                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h1 class="alert alert-danger">There is no data.</h1>
}
<br />
<hr />
<br />










<br />
<br />





